[pytest]
addopts = -ra -q
testpaths = tests releases/alpha90_1_2_3/tests
markers =
    unit: fast isolated unit tests
    contract: frozen scientific/software contract tests
    integration: small end-to-end tests with no expensive external campaigns
    scientific_validation: expensive benchmark/campaign validation; excluded from normal CI
