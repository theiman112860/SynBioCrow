# SynBioCrow V2.0 Alpha90.1.4.2.2

Canonical Molecular Identity Boundary & Route-Promotion Audit.

This bounded diagnostic leaves the frozen Alpha87.11 source and scientific parameters unchanged. It maps identity-equivalent target serializations to one deterministic RDKit canonical SMILES, instruments aliases of the exact frozen `predict_routes` callable plus exposed route predicates, and traces lower-level route objects into the autonomous `design()` path. It also runs all equivalent 1,4-butanediol inputs through the same external canonical boundary to test output invariance.

No chemistry, search-depth, timeout, ranking, sink, alias, or benchmark-specific tuning changes are permitted. No full Golden-20 rerun is authorized by this release. Packaged verification: 5/5 tests passed; notebook cells compile.