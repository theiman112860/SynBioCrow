# SynBioCrow V2.0 Alpha90.1.4.4.1

Golden Primary Reference Representation Archaeology.

This release recursively inventories the certified Golden primary supplement and reconstructs its archive nesting, pathway-group structure, schemas, identifier namespaces, order/step fields, and explicit reaction representations. It does not read sealed prediction routes and performs no benchmark scoring. Any deterministic bridge recovered here is only a candidate and must be independently frozen before exact-path scoring can resume.

No post-hoc aliases, gap reconstruction, prediction rewriting, benchmark tuning, or Alpha90.2 authorization is permitted.

Packaged verification: 5/5 tests passed; all notebook code cells compile.